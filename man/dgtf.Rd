% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgtf.R
\name{dgtf}
\alias{dgtf}
\title{Fit a DGTF model}
\usage{
dgtf(
  y,
  model,
  prior = dgtf_prior(),
  method = c("hva", "mcmc", "vb"),
  control = NULL,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{y}{Numeric vector of observed counts (or, in future versions,
a matrix for multivariate observations).}

\item{model}{A \code{\link{dgtf_model}} object.}

\item{prior}{A \code{\link{dgtf_prior}} object. The default is an empty
specification, under which all static parameters are held fixed.}

\item{method}{\code{"hva"} or \code{"mcmc"}. \code{"vb"} is accepted as a synonym
for \code{"hva"}, and the fitted object records the method as \code{"hva"}
either way.}

\item{control}{A method-specific control object. When \code{NULL}, the
defaults of the selected method are used.}

\item{seed}{Optional integer seed.}

\item{verbose}{If \code{TRUE}, returns the lowered model and method
settings \emph{without} calling the C++ engine, which is useful for
inspecting the arguments passed to the engines.}
}
\value{
An object of class \code{dgtf_fit}.
}
\description{
The user-facing entry point for inference on a DGTF model. The
function wraps the C++ inference engines and returns an S3
\code{dgtf_fit} object supporting the standard methods (\code{print},
\code{summary}, \code{coef}, \code{predict}, and others).
}
\section{Choosing a method}{

\itemize{
\item \code{"hva"} (or its synonym \code{"vb"}): the hybrid variational
approximation, which combines sequential Monte Carlo smoothing with
variational updates. It is the faster of the two methods and is well
suited to exploratory analysis and model screening. Tuned through
\code{\link[=vb_control]{vb_control()}}.
\item \code{"mcmc"}: a blocked sampler that updates the latent state
disturbances by Metropolis--Hastings and the static parameters by
Hamiltonian Monte Carlo. It targets the exact posterior and is the
reference method for uncertainty quantification, at a higher
computational cost. Tuned through \code{\link[=mcmc_control]{mcmc_control()}}.
}
}

\section{Inference rule}{

Anything supplied a prior in \code{prior} is treated as unknown and
inferred. Anything left out of \code{prior} is treated as fixed at the
value supplied to \code{\link[=dgtf_model]{dgtf_model()}}.
}

\examples{
\dontrun{
mod <- dgtf_hawkes()
sim <- dgtf_simulate_model(mod, ntime = 200, seed = 1)
fit <- dgtf(sim$y, mod,
            prior = dgtf_prior(W = inv_gamma(1, 1)),
            method = "hva",
            control = vb_control(iter = 200))
}
}
