% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior-predict.R
\name{posterior_predict}
\alias{posterior_predict}
\alias{posterior_predict.dgtf_fit}
\title{Posterior predictive draws}
\usage{
posterior_predict(object, ...)

\method{posterior_predict}{dgtf_fit}(object, nrep = 100L, Rt = NULL, level = 0.95, ...)
}
\arguments{
\item{object}{A \code{dgtf_fit}.}

\item{...}{Unused.}

\item{nrep}{Number of replicate draws \strong{per posterior sample}. The
total number of posterior predictive draws is \code{nrep} times the
number of posterior samples, where the latter is the second
dimension of \code{object$fit$psi_stored} or \code{object$fit$Theta}.}

\item{Rt}{Optional vector of reference intensity values, of the same
length as \code{y}, such as \code{softplus(sim$psi)} in a simulation study.
Pass \code{NULL} (default) to skip the reference comparison.}

\item{level}{Credible interval level used when reporting coverage,
interval width, and the interval score (default \code{0.95}).}
}
\value{
An object of class \code{dgtf_ppc} (a list). Four elements are
always present.
\itemize{
\item \code{chi}, the averaged chi-square discrepancy of \code{y}.
\item \code{Rt}, an \verb{(ntime + 1) x 3} matrix of \code{R_t} posterior quantiles at
\verb{(alpha/2, 0.5, 1 - alpha/2)} with \code{alpha = 1 - level}.
\item \code{width_Rt}, the mean width of the \code{R_t} credible bands.
\item \code{level}, the credible interval level used.
}

Three further elements are added when \code{nrep > 0}.
\itemize{
\item \code{crps}, the averaged continuous ranked probability score for \code{y}.
\item \code{yhat}, an \verb{(ntime + 1) x 3} quantile matrix of the posterior
predictive distribution of \code{y}.
\item \code{coverage_yhat}, \code{width_yhat}, and \code{interval_score_yhat}, the
Winkler interval score for \code{y} together with its components.
}

When a reference \code{Rt} trajectory is supplied, as in a simulation
study, the recovery metrics \code{mae_Rt}, \code{rmse_Rt}, \code{coverage_Rt}, and
\code{interval_score_Rt} are added as well.
}
\description{
Generates posterior predictive samples for an in-sample series under
the fitted model. Use this for posterior predictive checks and
goodness of fit assessment.
}
\examples{
\dontrun{
fit <- dgtf(sim$y, mod, prior, method = "hva", control = vb_control())
pp  <- posterior_predict(fit, nrep = 100)
}
}
